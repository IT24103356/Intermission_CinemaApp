# Copy to .env — not committed.
# Local:
EXPO_PUBLIC_API_URL=http://localhost:5000/api
# Netlify (Site settings → Environment variables): full HTTPS URL, e.g.
# EXPO_PUBLIC_API_URL=https://your-service.up.railway.app/api
